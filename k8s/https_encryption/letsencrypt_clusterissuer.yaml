apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory  # Production environment
    email: nurzhanat2002@gmail.com  # Replace with your email
    privateKeySecretRef:
      name: letsencrypt-prod-key  # Secret to store the private key
    solvers:
    - http01:
        ingress:
          class: nginx

